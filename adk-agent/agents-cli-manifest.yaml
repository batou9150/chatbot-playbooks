name: 'adk-agent'
acli_version: '1.6.1'
agent_directory: 'weather_time_agent'
region: 'us-east1'
base_template: 'local_adk-agent'
generated_at: '2026-09-21T14:44:10.539391+00:00'
language: 'python'
create_params:
  # The ADK name of the root agent, as rendered into the agent source. Rename
  # the agent and you must change it in both places: telemetry reports this as
  # gen_ai.agent.name, and it is what agents-cli reports for the project.
  root_agent_name: 'adk_agent'
  deployment_target: 'agent_runtime'
  session_type: 'in_memory'
  cicd_runner: 'skip'
  is_a2a: true
  agent_gateway: False
  agent_guidance_filename: 'GEMINI.md'
